# Core deep learning framework
# InceptionV3 requires TF >= 2.12; trained with TF >= 2.12 / Keras >= 2.12
tensorflow>=2.12

# Numerical computing
numpy>=1.24

# Image loading & preprocessing (LANCZOS resize used in inference)
Pillow>=10.0

# Evaluation metrics (accuracy, precision, recall, F1, AUC, ROC)
scikit-learn>=1.3

# HTTP requests (used by snippet capture utilities)
requests>=2.31

# Google Drive model downloader (used by download_model.py)
gdown>=4.7
# Flask for the browser-based detection dashboard
flask>=3.0
